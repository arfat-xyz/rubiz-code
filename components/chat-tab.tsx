"use client";
import { tabValueProps } from "@/utils/interface";
import React from "react";

const ChatTabComponent = ({ activeTab }: { activeTab: tabValueProps }) => {
  return <div>ChatTabComponent</div>;
};

export default ChatTabComponent;

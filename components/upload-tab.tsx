"use client";
import { tabValueProps } from "@/utils/interface";
import React from "react";

const UploadTabComponent = ({}: { activeTab: tabValueProps }) => {
  return <div>UploadTabComponent</div>;
};

export default UploadTabComponent;
